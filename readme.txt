Git is a version control system.
Git is nice software.
learning for now.
yeah! iknow 'git add <filename>' and 'git commit -m "<note>" is one process

	now i will use the command "git add" and "git commit" to commit the file
